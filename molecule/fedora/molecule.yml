---
dependency:
  name: galaxy

driver:
  name: docker

lint:
  name: yamllint

platforms:
  - name: ansible-role-git-credential-manager-fedora
    image: fedora:28

provisioner:
  name: ansible
  playbooks:
    converge: ../default/playbook.yml
  lint:
    name: ansible-lint

scenario:
  name: fedora

verifier:
  name: testinfra
  lint:
    name: flake8
